services:
  auth-db:
    image: alpine:latest
    container_name: sqlite_cli
    volumes:
      - ./db_data:/db
    command: >
      sh -c "apk add --no-cache sqlite && tail -f /dev/null"
    restart: unless-stopped

